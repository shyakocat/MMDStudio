{
  "name": "mmdstudio",
  "version": "0.0.2",
  "dependencies": {
    "electron": "^9.1.0",
    "iconv-lite": "^0.5.0",
    "jquery": "^3.4.1"
  },
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "pack": "electron-packager . MMDStudio --platform=win32 --arch=x64 --out=./build --app-version=0.0.2 --electron-version=4.0.3 --icon=./Thumbnail/cover.ico"
  },
  "devDependencies": {
    "electron-packager": "^14.0.6"
  }
}
